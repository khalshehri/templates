export const metadata = {
  title: "Safahati — Demo Lab",
  description: "Preview and test new design experiments",
};

export default function DemoLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
